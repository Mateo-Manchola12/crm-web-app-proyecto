---
import { tiers } from '../data/pricing'
import Dropdown from './ui/Dropdown.astro'
import GetDataForm from './ui/GetDataForm.astro'
---

<GetDataForm data-redirect="/contact" action="/contact" buttonText="Enviar" labelBy="contact-form-title" data-origin="contact">
    <Dropdown
        slot="custom-fields"
        label="Plan de interés"
        name="plan"
        placeholder="Seleccionar un plan"
        ariaLabel="Plan de interés"
        options={Object.keys(tiers).map((key) => ({
            value: key.toLowerCase(),
            label: key.charAt(0).toUpperCase() + key.slice(1),
        }))}
    />
</GetDataForm>
